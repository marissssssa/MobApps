# pubspec.yaml (letakkan di root: MobApps/pubspec.yaml)
name: mobapps
description: A Flutter project untuk “Homepage Guru”
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # Karena kamu tidak memakai aset gambar apa pun, bagian ini dikomentari:
  # assets:
  #   - assets/images/
